#include<stdio.h>
int main()
{
	int length,breadth,area;
	printf("\nenter the length");
	scanf("%d",&length);
	printf("\nenter the breadth");
	scanf("%d",&breadth);
	area=length*breadth;
	printf("%d",area);
}
